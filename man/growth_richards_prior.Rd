% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-growth-prior.R
\name{growth_richards_prior}
\alias{growth_richards_prior}
\title{Create a Prior for the Richards Growth Model}
\usage{
growth_richards_prior(
  K = brms::prior(prior = normal(1, 0.5), nlpar = "K"),
  K0 = brms::prior(prior = normal(0, 0.5), nlpar = "K0"),
  rate = brms::prior(prior = normal(2, 0.5), nlpar = "rate", lb = 0),
  lambda = brms::prior(prior = normal(0.5, 0.5), nlpar = "lambda"),
  nu = brms::prior(prior = normal(1, 1), nlpar = "nu", lb = 0),
  ...
)
}
\arguments{
\item{K}{\code{\link[brms:set_prior]{brms::brmsprior()}} or \code{numeric}. Prior for the
\code{K} parameter. (Default: \code{normal(1, 0.5)}).}

\item{K0}{\code{\link[brms:set_prior]{brms::brmsprior()}} or \code{numeric}. Prior for the
\code{K0} parameter. (Default: \code{normal(0, 0.5)}).}

\item{rate}{\code{\link[brms:set_prior]{brms::brmsprior()}} or \code{numeric}. Prior for the
\code{rate} parameter. (Default: \code{normal(1, 1)} with and a lower bound
of \code{0}).}

\item{lambda}{\code{\link[brms:set_prior]{brms::brmsprior()}} or \code{numeric}. Prior for the
\code{lambda} parameter. (Default: \code{normal(0.5, 0.5)}).}

\item{nu}{\code{\link[brms:set_prior]{brms::brmsprior()}} or \code{numeric}. Prior for the
\code{nu} parameter. (Default: normal(1, 1) with a lower bound of
\code{0}).}

\item{...}{additional \code{\link[brms:set_prior]{brms::brmsprior()}} objects.}
}
\value{
\code{\link[brms:set_prior]{brms::brmsprior()}} \code{data.frame}
}
\description{
Create a priors for the Richards growth model. If the
function arguments \code{K}, \code{K0}, \code{rate}, \code{lambda}, or \code{nu} are \code{NULL}, normal
distribution priors will be set.

If you would like to set a parameter as a constant, enter a numeric
value for the function argument.

For other distribution options, reference \link[brms:set_prior]{brms::prior} and \href{https://github.com/stan-dev/stan/wiki/Prior-Choice-Recommendations}{Prior Choice Recommendations }
}
\examples{
\dontrun{
priors <- growth_richards_prior(
  K = brms::prior(prior = normal(100, 20), nlpar = "K"),
  nu = brms::prior(prior = normal(2, 4), nlpar = "nu"),
  lambda = 1)
}
}
