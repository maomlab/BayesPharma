
```{r load-libraries}
library(plyr)
library(dplyr)
library(ggplot2)
library(scales)
```


setwd("~/Dropbox/Shoichet Lab/Large Scale Docking/Full Analysis")
source("src/analysis_parameters.R")

```{r generate-date}
data <- expand.grid(
  mu=c(.2, .4, .6, .8),
  size=c(10, 20, 100, 500),
  x=seq(0, 1, length.out=200)) %>%
  dplyr::mutate(
    p = dbeta(x=x, shape1=mu*size, shape2=(1-mu)*size))
```

```{r plot-functional-form}
ggplot2::ggplot(data=data) +
  ggplot2::theme_bw() +
  ggplot2::geom_line(aes(x=x, y=p, color=mu, group=mu), size=2) +
  ggplot2::facet_wrap(~size, scales="free_y") +
  ggplot2::scale_x_continuous("Hit Rate") +
  ggplot2::scale_y_continuous("Probability of Beta Distribution") +
  ggplot2::scale_color_continuous("Mean")
ggplot2::ggsave("product/beta_model.pdf", height=5, width=7)
ggplot2::ggsave("product/beta_model.png", height=5, width=7)

```

```{r }


```
